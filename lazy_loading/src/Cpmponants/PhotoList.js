import React, {useEffect} from 'react';
import Config from './../Config';

const PhotoList = (props) => {

    return(
        <div>
            this is PhotoList componenet
        </div>
    );
}
export default PhotoList;