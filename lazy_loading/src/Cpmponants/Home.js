import React  from 'react';
import { BrowserRouter, Route, Link } from "react-router-dom";

const Home = (props) => {
    return (
        <div>
            Welcome to react Js
        </div>
    );
  }

  export default Home;